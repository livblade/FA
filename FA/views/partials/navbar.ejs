<nav class="navbar"><div class="nav-container">
    <a href="/" class="logo">TRUSTMATE <span class="logo-icon">✶</span></a>
    
    <% if (typeof tasks!== 'undefined') { %>
        <div class="nav-search-pill">
            <form action="/tasks" method="GET" style="display:flex; width:100%; align-items:center;">
                <input type="text" name="query" placeholder="What Services are you looking for?" class="nav-search-input">
                <button type="submit" class="nav-search-btn">🔍</button>
            </form>
        </div>
    <% } %>

    <div class="nav-right">
        <div class="nav-item"><a href="/tasks">Explore <span class="arrow">▼</span></a></div>
        <div class="nav-item"><a href="/become-seller">Become a Seller</a></div>
        <% if (user) { %>
            <div class="nav-item">Hi, <strong><%= user.username %></strong></div>
            <div class="nav-item"><a href="/logout">Logout</a></div>
        <% } else { %>
            <div class="nav-item"><a href="/login">Login</a></div>
            <div class="nav-item"><a href="/signup" class="signup-btn">Sign up</a></div>
        <% } %>
    </div>
</div></nav>