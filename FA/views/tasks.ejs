<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Popular Services | TRUSTMATE</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="logo">TRUSTMATE <span class="logo-icon">✶</span></a>
            <div class="nav-right">
                <div class="search-pill nav-search">
                    <input type="text" placeholder="Search services..." class="search-input">
                    <button class="search-btn-dark nav-search-btn">🔍</button>
                </div>
                <div class="nav-item"><a href="/tasks">Explore ▼</a></div>
                <div class="nav-item"><a href="/signup" class="signup-btn">Sign up</a></div>
            </div>
        </div>
    </nav>
    <div class="page-container">
        <h1 class="main-title">Popular Services</h1>
        <div class="services-grid">
            <% tasks.forEach(task => { %>
                <div class="service-card">
                    <div class="card-header">
                        <div style="display:flex; justify-content:space-between; align-items:center;">
                            <span class="verified-tag">
                                <%= task.category %> <% if (task.is_verified) { %> ✓ Verified Neighbor <% } %>
                            </span>
                        </div>
                        <h3 style="margin:15px 0; font-weight:800;"><%= task.description %></h3>
                        <div style="font-size:26px; font-weight:800;">S$<%= task.price %></div>
                    </div>
                    <div class="card-footer">
                        <div class="rank-box">
                            Neighborhood Rank: <span style="font-weight:800; color:#000;"><%= (task.reputation_rank * 10).toFixed(1) %>/10</span>
                        </div>
                        <button class="btn-submit" style="margin-top:15px; border-radius:12px;">Book Now</button>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>