<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Trustmate - Register</title>

  <!-- Optional font (safe fallback if blocked) -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">

  <style>
    :root{
      --text:#111;
      --muted:#6b6b6b;
      --line:#d9d9d9;
      --nav:#f6f6f6;
      --btn:#0b0f2a;
      --btn2:#070a1f;
      --bg:#ffffff;
      --input:#e6e6e6;
    }

    *{ box-sizing:border-box; }
    body{
      margin:0;
      font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      color:var(--text);
      background:var(--bg);
    }

    /* Top header */
    .topbar{
      height:84px;
      background:var(--nav);
      border-bottom:1px solid var(--line);
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding:0 56px;
    }

    .brand{
      display:flex;
      align-items:center;
      gap:14px;
      font-weight:800;
      letter-spacing:1px;
      font-size:44px;
      line-height:1;
    }
    .brand .mark{
      width:34px;height:34px; display:inline-block;
    }

    .nav{
      display:flex;
      align-items:center;
      gap:22px;
      font-weight:600;
      color:#555;
    }
    .nav a{
      text-decoration:none;
      color:#666;
    }
    .nav a:hover{ color:#111; }
    .dropdown{
      display:inline-flex;
      align-items:center;
      gap:10px;
      color:#666;
    }
    .caret{
      width:0;height:0;
      border-left:6px solid transparent;
      border-right:6px solid transparent;
      border-top:7px solid #777;
      transform: translateY(2px);
    }

    /* Page frame */
    .frame{
      max-width:1180px;
      margin:0 auto;
      padding:34px 42px 60px;
    }

    .title{
      text-align:center;
      font-weight:800;
      letter-spacing:2px;
      font-size:34px;
      margin:8px 0 18px;
    }

    .content{
      display:grid;
      grid-template-columns: 1.1fr 0.9fr;
      gap:70px;
      align-items:start;
      margin-top:10px;
    }

    /* Left heart block */
    .heartWrap{
      display:flex;
      justify-content:center;
      align-items:center;
      padding-top:10px;
    }
    .heart{
      width:520px;
      max-width:100%;
      filter: drop-shadow(0 1px 0 rgba(0,0,0,0.03));
    }

    /* Right form */
    .panel{
      padding-top:6px;
    }

    .form{
      width:100%;
      max-width:520px;
      margin-left:auto;
    }

    .input{
      width:100%;
      height:46px;
      border:1px solid #cfcfcf;
      border-radius:6px;
      padding:0 14px;
      margin:0 0 16px;
      font-size:16px;
      outline:none;
      background:#fff;
    }
    .input:focus{
      border-color:#a9a9a9;
      box-shadow:0 0 0 3px rgba(0,0,0,0.06);
    }

    .btn{
      width:100%;
      height:54px;
      border:none;
      border-radius:6px;
      color:#fff;
      font-weight:700;
      letter-spacing:1px;
      font-size:18px;
      cursor:pointer;
      background: linear-gradient(180deg, var(--btn), var(--btn2));
      margin-top:4px;
    }
    .btn:hover{ filter: brightness(1.06); }
    .btn:active{ transform: translateY(1px); }

    .or{
      display:flex;
      align-items:center;
      gap:14px;
      margin:18px 0 12px;
      color:#333;
      font-weight:600;
    }
    .or .line{
      flex:1;
      height:1px;
      background:#cfcfcf;
    }
    .or span{
      font-size:13px;
      letter-spacing:1px;
      color:#333;
    }

    .loginRow{
      text-align:center;
      margin:6px 0 14px;
      font-size:20px;
      font-weight:700;
    }
    .loginRow a{
      color:#111;
      text-decoration:none;
      font-weight:800;
    }
    .loginRow a:hover{ text-decoration:underline; }

    .terms{
      display:flex;
      gap:12px;
      align-items:flex-start;
      font-size:16px;
      color:#222;
    }
    .terms input{
      margin-top:4px;
      width:20px; height:20px;
      accent-color:#111;
    }
    .terms .small{
      line-height:1.35;
    }

    /* Responsive */
    @media (max-width: 980px){
      .topbar{ padding:0 20px; }
      .brand{ font-size:34px; }
      .content{
        grid-template-columns:1fr;
        gap:26px;
      }
      .form{ margin:0 auto; }
    }
  </style>
</head>

<body>
  <header class="topbar">
    <div class="brand">
      <div>TRUSTMATE</div>

      <!-- Small star/flower mark -->
      <svg class="mark" viewBox="0 0 64 64" aria-hidden="true">
        <g fill="#111">
          <path d="M31 2h2v60h-2z"/>
          <path d="M2 31h60v2H2z"/>
          <path d="M10 10l1.4-1.4L54 51.2 52.6 52.6z"/>
          <path d="M52.6 10L54 11.4 11.4 54 10 52.6z"/>
          <path d="M6 22l.7-1.9 52.6 20-.7 1.9z"/>
          <path d="M6.7 42.9l-.7-1.9 52.6-20 .7 1.9z"/>
        </g>
      </svg>
    </div>

    <nav class="nav">
      <span class="dropdown">Trustmate PRO <i class="caret"></i></span>
      <span class="dropdown">Explore <i class="caret"></i></span>
      <a href="/become_a_seller">Become a Seller</a>
      <a href="/login">Login</a>
      <a href="/register">Sign up</a>
    </nav>
  </header>

  <main class="frame">
    <div class="title">REGISTER</div>

    <section class="content">
      <!-- LEFT: Heart -->
      <div class="heartWrap">
        <!-- Big heart SVG with “Support Local” -->
        <svg class="heart" viewBox="0 0 720 560" role="img" aria-label="Support Local">
          <!-- Heart shape -->
          <path
            d="M360 520
               C 360 520, 70 350, 70 190
               C 70 110, 130 60, 205 60
               C 270 60, 320 95, 360 140
               C 400 95, 450 60, 515 60
               C 590 60, 650 110, 650 190
               C 650 350, 360 520, 360 520 Z"
            fill="#D72A88"
          />

          <!-- “Support” dark script-ish -->
          <text x="215" y="265"
                font-size="110"
                font-weight="800"
                fill="#2A1946"
                transform="rotate(-6 215 265)"
                style="font-family: 'Inter', system-ui, sans-serif;">
            Support
          </text>

          <!-- “Local” white big -->
          <text x="230" y="385"
                font-size="140"
                font-weight="900"
                fill="#ffffff"
                transform="rotate(-6 230 385)"
                style="font-family: 'Inter', system-ui, sans-serif;">
            Local
          </text>

          <!-- Small white accent line to mimic style -->
          <path d="M420 250 C 470 270, 500 315, 492 350"
                fill="none"
                stroke="#fff"
                stroke-width="18"
                stroke-linecap="round"
                opacity="0.95"/>
        </svg>
      </div>

      <!-- RIGHT: Form -->
      <div class="panel">
        <form class="form" onsubmit="event.preventDefault();">
          <input class="input" type="text" placeholder="Username" />
          <input class="input" type="text" placeholder="Phone Number/email" />
          <input class="input" type="text" placeholder="verification code" />
          <input class="input" type="password" placeholder="password" />
          <input class="input" type="password" placeholder="confirm password" />

          <button class="btn" type="submit">NEXT</button>

          <div class="or">
            <div class="line"></div>
            <span>OR WITH</span>
            <div class="line"></div>
          </div>

          <div class="loginRow">
            Have an account? <a href="login">login</a>
          </div>

          <label class="terms">
            <input type="checkbox"/>
            <div class="small">
              By registering, You agree to the Terms,<br/>
              Conditions and Policies of&nbsp; Local Help
            </div>
          </label>
        </form>
      </div>
    </section>
  </main>
</body>
</html>
