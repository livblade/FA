<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Popular Services | TRUSTMATE</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="logo">TRUSTMATE <span class="logo-icon">✶</span></a>
            
            <div class="nav-search-wrapper">
                <div class="nav-search-pill">
                    <input type="text" placeholder="What Services are you looking for?" class="nav-search-input">
                    <button class="nav-search-btn">🔍</button>
                </div>
            </div>

            <div class="nav-right">
                <div class="nav-item">
                    <a href="/tasks">Explore <span class="arrow">▼</span></a>
                </div>
                <div class="nav-item">
                    <a href="#">Become a Seller</a>
                </div>
                <div class="nav-item">
                    <a href="/login">Login</a>
                </div>
                <div class="nav-item">
                    <a href="/signup" class="signup-btn">Sign up</a>
                </div>
            </div>
        </div>
    </nav>

    <div class="page-container">
        <h1 class="main-title">Popular Services</h1>
        <div class="services-grid">
            <% tasks.forEach(task => { %>
                <div class="service-card">
                    <div class="card-header">
                        <div style="display:flex; justify-content:space-between; align-items:center;">
                            <span class="cat-tag">
                                <%= task.category %>
                            </span>
                            <% if (task.is_verified) { %>
                                <span class="verified-tag">✓ Verified Neighbor</span>
                            <% } %>
                        </div>
                        <h3 style="margin:15px 0; font-weight:800; line-height:1.2;">
                            <%= task.description %>
                        </h3>
                        <div style="font-size:26px; font-weight:800;">
                            S$<%= task.price %>
                        </div>
                    </div>
                    
                    <div class="card-footer">
                        <div class="rank-box">
                            Neighborhood Rank: 
                            <span style="font-weight:800; color:#000;">
                                <%= (task.reputation_rank * 10).toFixed(1) %>/10
                            </span>
                        </div>
                        <button class="btn-auth-submit" style="width:100%; border-radius:12px; margin-top:15px; height:50px; text-transform: none;">
                            Book Now
                        </button>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>